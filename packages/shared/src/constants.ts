import type { GroupKey } from './types';

export const GROUP_KEYS: GroupKey[] = ['people', 'elites', 'security'];

export const STAT_MIN = 0;

export const STAT_MAX = 10;

export const MAX_HAND_CARDS = 8;

export const PLAYER_ACTIONS_PER_ROUND = 3;

export const OPENING_HAND_CARDS = 5;
